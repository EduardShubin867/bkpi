# Validation
Isolated temp directories only; never read actual credentials or mutate real agent config during tests. Mock Bitrix HTTP for pagination, retry, snapshot/team and secret echo redaction. Test malformed/escaped/symlink paths, person/self resolution, state writes and migration preservation. Exercise real binary stdio and JSON output. Installer tests mock command runner and HTTP; local npm pack/npx flow must work without Rust installation. Run fmt, clippy all targets/features, cargo test, release build, npm typecheck/lint/test/build. Real portals, OS credential UIs and other OS binaries require explicit environment availability.
