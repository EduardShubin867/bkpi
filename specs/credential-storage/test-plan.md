Test file CRUD, atomic replacement, Unix 0600 and insecure/symlink rejection; env present/missing; schema backups and keyring compatibility; mock move ordering/failures/shared refs; legacy migration; central URL/key/value redaction and secret-free doctor. Installer prompt choices, preserved backend, safe child output/failure. Run cargo fmt --check, clippy all-targets/all-features -D warnings, test default/all-features, release build; npm typecheck/lint/test/build. No live credentials or external writes. CLI only: browser checks do not apply.
