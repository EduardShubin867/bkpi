Extend the existing tagged CredentialRef with env; retain keyring spelling for compatibility while presenting keychain in UX. Keep per-account secrets/<account>.secret files under dirs::config_dir()/bkpi (existing abstraction and paths), atomic replacement and owner-only Unix modes. Schema 2 supports env; schema 1 loads read-only and upgrades with backup at next save. Backend switching writes and verifies destination, commits config, then best-effort deletes unshared source. Env moves require a present valid webhook and --confirm-env; never write environment. No workspace/person/integration model rewrite.
